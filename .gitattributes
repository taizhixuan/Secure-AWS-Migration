# Normalize line endings. Shell scripts MUST be LF (CRLF breaks bash).
* text=auto

*.sh   text eol=lf
*.ps1  text eol=crlf

# Binary assets — never line-ending-converted.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.pdf  binary
*.docx binary
